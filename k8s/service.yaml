apiVersion: v1
kind: Service
metadata:
  name: costlens-backend-svc
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: costlens-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
